0.
Написать программу, которая состоит из 3-х функций: 1 - Функция, в которую передаешь параметры, она производит любые действия (например складывает числа), возвращает результат, 2 - Функция, которая не принимает никаких параметров, но внутри себя берет одну или несколько переменных из глобальной области видимости (извне функции), что-то с ними делает, возвращает результат, 3 - Функция, которая не принимает никаких аргументов, ничего не возвращает, но внутри производит какие-то действия, например выводит на экран произвольный текст. Программа должна демонстрировать пользователю (выдавать на экран) информацию о том какую функцию ты сейчас запускаешь, что она делает, результаты её работы.

1.
Написать программу, которая из строки "1 2 3 4 5 6 7 8 9 0" делает массив чисел вида: []int{1,2,3,4,5,6,7,8,9,0}, потом перебирает с помощью цикла все элементы массива, если число четное - выводит на экран информацию о том какой порядковый номер (индекс) у этого числа в массиве и само число, нечетные числа складывает в другую переменную, после окончания цикла выводит на экран этот массив с нечетными числами

2.
Пользователь вводит число от 1 до 9999 (сумму выдачи в банкомате). Необходимо вывести на экран словами введенную сумму и в конце написать название валюты с правильным окончанием. Например: 7431 – семь тысяч четыреста тридцать один доллар, 2149 – две тысячи сто сорок девять долларов, 15 – пятнадцать долларов, 3 – три доллара.﻿

3. задача на рекурсию
Организовать ввод чисел до тех пор, пока пользователь не введёт ноль. Разрешаем на вход числа от 0 до 9, если вписал больше, то ругаемся, но ввод не завершаем.

4.
Написать функцию, которая сравнивает два слайса.

5.
Создаем переменную с типом интерфейс и кладем туда какое-нибудь значение. С помощью switch определить тип переменной.

6.
Дано: строка a := «mfgah134517095aldrfgvh8h».
Вырежьте из строки всё, кроме чисел.

7.
Дано: срезы a := []int{1, 2, 3}
b := []int{4, 5, 6}.
Выполните конкатенацию двух срезов.

8.
Преобразуйте строку в число.

9.
Проверьте, что файл существует.

10.
Имеется слайс чисел []int{794, 429, 794, 794, 794, 794, 1364, 794, 794, 1019, 794, 670, 794, 794, 794, 794, 794, 794, 738, 794, 557, 794, 794}. Необходимо высчитать среднее значение всех чисел.

11.
Найти в слайсе целых чисел все последовательности, сохранить их индексы и вывести в конце.

12. Сгенерировать csv файл с данными.